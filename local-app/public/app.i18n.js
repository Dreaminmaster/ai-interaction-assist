const messages = {
  'zh-CN': {
    panel_config_title: '主题与配置', continue_learning: '继续上次学习', export_backup: '导出学习备份', import_backup: '导入学习备份', language: '语言',
    create_topic: '新建学习主题', title_placeholder: '主题名称，例如 PID 控制', goal_placeholder: '学习目标，例如理解 PID 三项作用并能口头解释',
    mode_basic: '补基础模式', mode_understand: '理解模式', mode_exam: '考试模式', mode_defense: '答辩模式', mode_paper: '论文模式', mode_project: '项目模式',
    create_topic_button: '创建主题', recent_topics: '最近主题', current_topic: '当前主题', topic_profile: '主题档案', current_mode: '当前模式', current_stage: '当前阶段',
    current_status: '当前状态', next_entry: '下次入口', current_weak_points: '当前薄弱点', no_weak_points: '当前还没有记录薄弱点。', current_session: '本轮学习',
    start_session: '开始本轮学习', current_questions: '本轮问题', start_session_first: '请先开始本轮学习。', session_summary_title: '本轮沉淀', summary: '本轮总结',
    mastered_points: '已掌握', no_mastered_points: '当前还没有记录已掌握点。', session_weak_points: '当前薄弱点', answer_questions: '回答本轮问题',
    answer_placeholder: '在这里写下你的理解、例子或当前困惑。', submit_answer: '提交回答并更新本轮沉淀', recent_sessions: '最近记录', no_sessions: '当前主题还没有历史学习记录。',
    status: '状态', local_web_mode: '本地网页模式', local_web_mode_desc: '当前入口带导入恢复和导出备份按钮，学习记录会通过本地服务保存到 data/state.json。',
    waiting: '等待开始本轮学习。', provider_label: '当前 provider：', analyzing: '正在分析当前回答…', import_success: '导入成功，页面已恢复学习状态。', import_fail: '导入失败。',
    no_goal_prefix: '目标：', no_next_prefix: '下一步：', recent_empty: '当前主题还没有历史学习记录。', session_summary_default: '开始一轮学习后，这里会出现总结。'
  },
  en: {
    panel_config_title: 'Topics & Settings', continue_learning: 'Continue Last Learning', export_backup: 'Export Backup', import_backup: 'Import Backup', language: 'Language',
    create_topic: 'Create Topic', title_placeholder: 'Topic title, e.g. PID Control', goal_placeholder: 'Learning goal, e.g. explain PID terms in your own words',
    mode_basic: 'Foundation Mode', mode_understand: 'Understanding Mode', mode_exam: 'Exam Mode', mode_defense: 'Defense Mode', mode_paper: 'Paper Mode', mode_project: 'Project Mode',
    create_topic_button: 'Create Topic', recent_topics: 'Recent Topics', current_topic: 'Current Topic', topic_profile: 'Topic Profile', current_mode: 'Current Mode', current_stage: 'Current Stage',
    current_status: 'Current Status', next_entry: 'Next Entry', current_weak_points: 'Current Weak Points', no_weak_points: 'No weak points recorded yet.', current_session: 'Current Session',
    start_session: 'Start Session', current_questions: 'Current Questions', start_session_first: 'Start a learning session first.', session_summary_title: 'Session Summary', summary: 'Summary',
    mastered_points: 'Mastered Points', no_mastered_points: 'No mastered points recorded yet.', session_weak_points: 'Current Weak Points', answer_questions: 'Answer Current Questions',
    answer_placeholder: 'Write your understanding, examples, or current confusion here.', submit_answer: 'Submit Answer and Update Summary', recent_sessions: 'Recent Sessions', no_sessions: 'No learning records for the current topic yet.',
    status: 'Status', local_web_mode: 'Local Web Mode', local_web_mode_desc: 'This entry includes import and export buttons, and learning records are saved to data/state.json through the local service.',
    waiting: 'Waiting to start a learning session.', provider_label: 'Current provider: ', analyzing: 'Analyzing the current answer…', import_success: 'Import succeeded. Learning state restored.', import_fail: 'Import failed.',
    no_goal_prefix: 'Goal: ', no_next_prefix: 'Next: ', recent_empty: 'No learning records for the current topic yet.', session_summary_default: 'A summary will appear here after you start a session.'
  }
};

const seedTopics = [{ id:'topic-pid', title:'PID 控制', goal:'理解 PID 三项各自作用，并能口头解释。', mode:'理解模式', currentStage:'基础理解', status:'进行中', lastSummary:'已经理解 P 和 I 的基本作用，D 项还不稳定。', nextEntry:'从 D 项的直观作用开始继续。', weakPoints:['微分项的直观作用','三项配合的口头表达'], updatedAt:new Date().toISOString() }];
let state = { lang:'zh-CN', provider:'mock', topics:[], sessions:[], selectedTopicId:'', activeSessionId:'', isAnalyzing:false };
const $ = (id) => document.getElementById(id);
const t = (key) => messages[state.lang][key] || key;
async function fetchJson(url, options={}) { const r = await fetch(url, options); if (!r.ok) throw new Error(await r.text() || `Request failed: ${r.status}`); return r.json(); }
async function loadStateFromFile(){ try { const s = await fetchJson('/api/state'); state.topics = Array.isArray(s.topics)&&s.topics.length?s.topics:seedTopics; state.sessions = Array.isArray(s.sessions)?s.sessions:[]; } catch { state.topics = seedTopics; state.sessions = []; await syncStateToFile(); } state.selectedTopicId = state.topics[0]?.id || ''; state.activeSessionId = getRecentSessions()[0]?.id || ''; }
async function syncStateToFile(){ await fetchJson('/api/state',{ method:'PUT', headers:{'Content-Type':'application/json'}, body:JSON.stringify({topics:state.topics,sessions:state.sessions}) }); }
function getSelectedTopic(){ return state.topics.find(t=>t.id===state.selectedTopicId)||state.topics[0]||null; }
function getActiveSession(){ return state.sessions.find(s=>s.id===state.activeSessionId)||null; }
function getRecentSessions(){ const topic=getSelectedTopic(); if(!topic) return []; return state.sessions.filter(s=>s.topicId===topic.id).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt)).slice(0,5); }
function createTopic(title,goal,mode){ return { id:`topic-${crypto.randomUUID()}`, title, goal, mode, currentStage: state.lang==='en' ? 'Just created' : '刚创建', status: state.lang==='en' ? 'In Progress' : '进行中', lastSummary: state.lang==='en' ? 'Topic created. Waiting for first diagnosis.' : '主题刚刚创建，等待首轮基础诊断。', nextEntry: state.lang==='en' ? 'Answer 3 to 5 basic diagnosis questions first.' : '先做 3 到 5 个基础诊断问题。', weakPoints:[], updatedAt:new Date().toISOString() }; }
function createInitialSession(topic){ return { id:`session-${crypto.randomUUID()}`, topicId:topic.id, title:`${topic.title} · ${state.lang==='en'?'Initial Diagnosis':'首轮基础诊断'}`, objective: state.lang==='en' ? `Complete the first diagnosis around “${topic.title}” and find the learning entry point.` : `围绕“${topic.title}”完成首轮基础判断，并确定学习起点。`, summary: state.lang==='en' ? 'Session started. Waiting for your answers.' : '本轮尚未完成，等待用户回答基础诊断问题。', masteredPoints:[], weakPoints:[], reviewQuestions: state.lang==='en' ? [`Have you learned “${topic.title}” before? How would you explain it in your own words?`,`What is the one problem you want to solve first about “${topic.title}”?`,`If you start learning “${topic.title}” now, where do you think you would get stuck first?`] : [`你之前接触过“${topic.title}”吗？你会怎么用自己的话解释它？`,`围绕“${topic.title}”，你现在最想先解决的一个问题是什么？`,`如果让你现在马上开始学“${topic.title}”，你觉得自己最容易卡住的地方是什么？`], nextEntry: state.lang==='en' ? 'Finish 3 to 5 diagnosis questions first, then generate a learning map.' : '先完成 3 到 5 个基础诊断问题，再生成学习地图。', createdAt:new Date().toISOString() }; }
function syncTopicFromSession(topic,session){ return {...topic,lastSummary:session.summary,nextEntry:session.nextEntry,weakPoints:session.weakPoints,currentStage: session.masteredPoints.length ? `${topic.currentStage} · ${state.lang==='en'?'Advanced':'已推进'}` : (state.lang==='en'?'Diagnosing':'基础诊断中'),updatedAt:new Date().toISOString()}; }
async function analyzeAnswer(topic,session,answer){ return fetchJson('/api/analyze-topic-answer',{ method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({provider:state.provider,topic,session,answer}) }); }
function applyI18n(){ document.querySelectorAll('[data-i18n]').forEach(el=>{ el.textContent = t(el.getAttribute('data-i18n')); }); document.querySelectorAll('[data-i18n-placeholder]').forEach(el=>{ el.setAttribute('placeholder', t(el.getAttribute('data-i18n-placeholder'))); }); document.documentElement.lang = state.lang==='en'?'en':'zh-CN'; document.title = state.lang==='en' ? 'AI Interactive Learning System' : 'AI 交互式学习系统'; }
function renderTopicList(){ const wrap=$('topicList'); wrap.innerHTML=''; state.topics.forEach(topic=>{ const btn=document.createElement('button'); btn.type='button'; btn.className=`topic-card ${topic.id===state.selectedTopicId?'topic-card-active':''}`; btn.innerHTML=`<div class="topic-card-title">${topic.title}</div><div class="topic-card-status">${topic.status} · ${topic.mode}</div><div class="topic-card-text">${topic.lastSummary}</div><div class="topic-card-next">${topic.nextEntry}</div>`; btn.onclick=()=>{ state.selectedTopicId=topic.id; state.activeSessionId=getRecentSessions()[0]?.id||''; render(); }; wrap.appendChild(btn); }); }
function setList(id,items,emptyText){ const el=$(id); el.innerHTML=''; (items&&items.length?items:[emptyText]).forEach(item=>{ const li=document.createElement('li'); li.textContent=item; el.appendChild(li); }); }
function renderTopicMeta(){ const topic=getSelectedTopic(); $('topicTitle').textContent=topic?.title || (state.lang==='en'?'AI Interactive Learning System':'AI 交互式学习系统'); $('topicGoal').textContent=topic?.goal || (state.lang==='en'?'A one-click local long-term learning web app.':'本地一键打开的长期学习网页应用。'); $('topicMode').textContent=topic?.mode || '-'; $('topicStage').textContent=topic?.currentStage || '-'; $('topicStatus').textContent=topic?.status || '-'; $('topicNext').textContent=topic?.nextEntry || '-'; setList('weakPointList',topic?.weakPoints||[],t('no_weak_points')); $('sessionTopicTitle').textContent=topic?.title||'-'; $('sessionTopicGoal').textContent=topic ? `${t('no_goal_prefix')}${topic.goal}` : '-'; $('sessionTopicNext').textContent=topic ? `${t('no_next_prefix')}${topic.nextEntry}` : '-'; }
function renderSession(){ const s=getActiveSession(); const questions=s?.reviewQuestions?.length?s.reviewQuestions:[t('start_session_first')]; const qWrap=$('questionList'); qWrap.innerHTML=''; questions.forEach(q=>{ const li=document.createElement('li'); li.textContent=q; qWrap.appendChild(li); }); $('sessionSummary').textContent=s?.summary || t('session_summary_default'); $('sessionNext').textContent=s?.nextEntry || '-'; setList('masteredList',s?.masteredPoints||[],t('no_mastered_points')); setList('sessionWeakList',s?.weakPoints||[],t('no_weak_points')); }
function renderRecentSessions(){ const wrap=$('recentSessionList'); const sessions=getRecentSessions(); wrap.innerHTML=''; if(!sessions.length){ wrap.innerHTML=`<div class="empty-state">${t('recent_empty')}</div>`; return; } sessions.forEach(s=>{ const div=document.createElement('div'); div.className='session-card'; div.innerHTML=`<div class="session-title">${s.title}</div><div class="session-summary">${s.summary}</div><div class="session-next">${t('next_entry')}：${s.nextEntry}</div>`; wrap.appendChild(div); }); }
function renderStatus(message){ $('statusText').textContent=message; $('submitAnswerBtn').disabled=state.isAnalyzing; $('answerInput').disabled=state.isAnalyzing; }
function render(){ applyI18n(); renderTopicList(); renderTopicMeta(); renderSession(); renderRecentSessions(); renderStatus(state.isAnalyzing?t('analyzing'):`${t('provider_label')}${state.provider}`); $('providerSelect').value=state.provider; $('languageSelect').value=state.lang; }
function bindEvents(){ $('providerSelect').addEventListener('change',e=>{ state.provider=e.target.value; render(); }); $('languageSelect').addEventListener('change',e=>{ state.lang=e.target.value; render(); }); $('exportBtn').addEventListener('click',()=>{ window.location.href='/api/export-state'; }); $('importFileInput').addEventListener('change', async (e)=>{ const file=e.target.files?.[0]; if(!file) return; try { const text = await file.text(); const parsed = JSON.parse(text); await fetchJson('/api/import-state',{ method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(parsed) }); await loadStateFromFile(); renderStatus(t('import_success')); render(); } catch { renderStatus(t('import_fail')); } finally { e.target.value=''; } }); $('topicForm').addEventListener('submit', async (e)=>{ e.preventDefault(); const title=$('titleInput').value.trim(); const goal=$('goalInput').value.trim(); const mode=$('modeInput').value; if(!title||!goal) return; const topic=createTopic(title,goal,mode); state.topics.unshift(topic); state.selectedTopicId=topic.id; await syncStateToFile(); $('titleInput').value=''; $('goalInput').value=''; render(); }); $('continueBtn').addEventListener('click',()=>render()); $('startSessionBtn').addEventListener('click', async ()=>{ const topic=getSelectedTopic(); if(!topic) return; const session=createInitialSession(topic); state.sessions.unshift(session); state.activeSessionId=session.id; await syncStateToFile(); render(); }); $('answerForm').addEventListener('submit', async (e)=>{ e.preventDefault(); const topic=getSelectedTopic(); const session=getActiveSession(); const answer=$('answerInput').value.trim(); if(!topic||!session||!answer) return; state.isAnalyzing=true; renderStatus(t('analyzing')); try { const result=await analyzeAnswer(topic,session,answer); const updatedSession={...session,summary:result.summary,masteredPoints:result.masteredPoints,weakPoints:result.weakPoints,nextEntry:result.nextEntry,reviewQuestions:result.reviewQuestions}; state.sessions=state.sessions.map(s=>s.id===session.id?updatedSession:s); state.topics=state.topics.map(tp=>tp.id===topic.id?syncTopicFromSession(tp,updatedSession):tp); await syncStateToFile(); $('answerInput').value=''; } catch (error) { renderStatus(error instanceof Error ? error.message : 'Analysis failed'); } finally { state.isAnalyzing=false; render(); } }); }
await loadStateFromFile(); bindEvents(); render();
